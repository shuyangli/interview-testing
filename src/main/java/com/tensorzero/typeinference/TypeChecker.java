package com.tensorzero.typeinference;

import java.util.List;

/**
 * PART 2 - TYPE CHECKING
 *
 * Provides functionality for type checking.
 */
public class TypeChecker {

    /**
     * Check if the given argument types are compatible with the function type.
     *
     * @param functionType the function type to check against
     * @param argumentTypes the list of argument types to validate
     * @return true if the arguments are type-compatible with the function, false otherwise
     */
    public static boolean typeCheck(FunctionType functionType, List<TypeNode> argumentTypes) {
        throw new UnsupportedOperationException("Not implemented yet"); // TODO
    }
}
